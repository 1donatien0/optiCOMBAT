# Scripts dev locaux (machine de dev uniquement — ne pas publier sur GitHub)
# Lanceurs racine
fix-dev.cmd
sign-dev.cmd
install-dev-trust.cmd
install-dotnet8-desktop.cmd
dev-fix.log

# Scripts PowerShell / batch dev
scripts/ensure-dev-trust.ps1
scripts/setup-dev-trust-admin.ps1
scripts/setup-dev-sign.ps1
scripts/show-crash.ps1
scripts/sign-dev-local.ps1
scripts/unblock-dev-build.ps1
scripts/fix-dev-all.ps1
scripts/verify-dotnet-desktop.ps1
scripts/stop-opticombat-dev.ps1
scripts/_sign-now.cmd
shield optiCOMBAT.png
logo etendu.png
NOUVEAU LOGO.png
OKAY.png
SET DES LOGO.png
assets/branding/mockup-okay-reference.png


# Visual Studio / MSBuild
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
*.rsuser
*.DotSettings.user
Directory.Build.props.user

# JetBrains Rider
.idea/

# Logs
*.log

# NuGet
packages/
*.nupkg

# Cache
*.cache

# Microsoft Word (audits / brouillons — synthèse officielle : README.md §12)
*.docx
audit_optiCombat_v8.docx
~$*.docx

# Audits Markdown (rapports de session / revues ponctuelles — ne pas versionner)
# Doc produit versionnée : README.md, SECURITY.md, docs/*.md, native/**/README.md
AUDIT_*.md
audit_*.md
RAPPORT_*.md
rapport_*.md

# Sorties de test / publish locales (racine)
test-*.txt
TestResults/
artifacts/
_build_verify/
sbom/
coverage/
*.opencover.xml

# Raccourcis Windows locaux (non versionnés)
*.lnk
Thumbs.db
Desktop.ini

# Secrets / signature (ne jamais committer)
.env
.env.*
*.pfx
*.p12
*.snk
secrets.json
credentials.json

# Binaires ClamAV / YARA (téléchargés via scripts/fetch-runtime-deps.ps1)
runtime/clamav/x64/*.exe
runtime/clamav/x64/*.dll
runtime/clamav/x64/freshclam.conf
runtime/clamav/x64/certs/
runtime/clamav/x86/freshclam.conf
runtime/clamav/database/freshclam-ci.log
runtime/clamav/database/freshclam.log
runtime/clamav/database/*.log
runtime/clamav/x86/*.exe
runtime/clamav/x86/*.dll
runtime/clamav/database/*.cvd
runtime/clamav/database/*.cld
runtime/clamav/database/*.cud
runtime/yara/*.exe
publish/

# Qualification panel (échantillons générés / malware réel / rapports)
qualification/generated/
qualification/malicious/
qualification/reports/

# Cache YARA compilé (généré au runtime / warmup — les .yar sources restent versionnés)
optiCombat/rules/_compiled.yarc
optiCombat/rules/_compiled.stamp
optiCombat/rules/.rules_count.cache

# Builds natifs MSVC / WDK (scripts/build-amsi.ps1, minifilter)
native/**/x64/
native/**/x86/
native/**/ARM64/
native/**/Debug/
native/**/Release/
native/**/*.obj
native/**/*.pch
native/**/*.pdb
native/**/*.ilk
native/**/*.exp
native/**/*.lib
native/**/*.tlog
native/**/*.idb
native/**/*.sys
native/**/*.cat

# Dossier output de l'installer
installer/output/

# Échantillons EICAR temporaires (tests d'intégration YARA)
.eicar-test/

# Sorties de build .NET (bin/obj) — ne jamais versionner
[Bb]in/
[Oo]bj/

# Scripts de signature utilitaires (dev local)
scripts/_sign-all.cmd
scripts/_sign-all.ps1

# Certificats / clés (jamais sur le dépôt)
*.cer
*.crt
*.key
*.pem

# Scripts d'extraction / génération d'icônes (outillage local uniquement)
scripts/extract-exe-icon.ps1
scripts/generate-brand-assets.py
scripts/refresh-shell-icons.ps1
scripts/sync-installer-icon.ps1
scripts/__pycache__/

# Fichiers de sauvegarde et logs locaux (jamais versionnés)
*.bak
/_release-build.log
/sign-all.log
/sign-now.log
